from django.apps import AppConfig


class PlatformMessagingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.platform_messaging"
    verbose_name = "Platform messaging"
